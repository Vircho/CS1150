import java.io.File;
import java.io.IOException;
import java.io.PrintWriter;
import java.util.Scanner;

public class InAppNotes {
	public static void main(String[] args) throws IOException {
		//Final Exam Review
		
		//Chapter 1
		
		/*
		 * Difference between compiler, logical, and runtime errors ->
		 * Compiler: Error when translated to machine code, missing ;, missing {, etc.
		 * Logical: Error with the logic, runs, but doesn't do what you want.
		 * Runtime: Error during runtime, /0, [array.length + 1], etc.
		 * Occurs at: compile, runtime, runtime
		 */
		
		//Chapter 2
		
		/*
		 * Print to console and obtain input from keyboard ->
		 * System.out.println("Hello world!"); int inputtedNumber = userInput.nextInt();
		 * 
		 * Know the different primitive data types ->
		 * int - whole numbers; double - numbers with decimals; char - single character; boolean - true or false
		 * 
		 * For variables: know how to declare, assign values to, use in statements ->
		 * int intVariable; intVariable = 1; System.out.println(intVariable);
		 * 
		 * For constants: know how to declare, assign values to, use in statements ->
		 * final int CONSTANT_INT = 2; System.out.println(CONSTANT_INT);
		 * 
		 * Be able to use the numeric operations (+, -, *, /, %) and the increment and decrement operators
		 * (++, --) ->
		 * int newInt = intVariable + CONSTANT_INT; newInt = newInt - 6; newInt = newInt * 2;
		 * newInt = newInt / 9; newInt = newInt % 2; newInt++; newInt--;
		 */
		
		//Chapter 3
		
		/*
		 * 
		 */
		
		
	}
}